# Build output
bin/
obj/

# IDE / tool config
*.user
.vs/
.vscode/
.idea/
.claude/

# Generated artifacts (kept locally, regenerated at runtime)
**/wwwroot/roadmaps/*.pdf

# Local config — real secrets live in dotnet user-secrets, never committed
appsettings.Development.json
appsettings.Local.json

# Local tooling (cloudflared.exe is 54 MB — download per the README instead)
tools/

# Personal marketing / content drafts (LinkedIn posts, etc.) — stay local.
# Anchored to repo root so it doesn't also swallow web/src/content/ (the
# Astro content collections, which ARE tracked).
/content/

# Logs and scratch files
*.log
.tunnel.stdout
.q.sql
